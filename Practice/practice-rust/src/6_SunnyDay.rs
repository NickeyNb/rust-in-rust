// 6- Create a boolean variable that represents whether it's sunny today. Print a message based on the value.
// Change file to main.rs

fn main () {
    let var = true;
    if var {
        println!("Its a sunnny day !");
    } else {
        println!("Not a sunny day ! ");
    }
}